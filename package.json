{
  "name": "youtubecontroller",
  "version": "1.0.0",
  "description": "A wrapper for the Youtube IFrame API that provides abstractions for treating videos/subsections of videos as 'tracks' in a 'playlist'",
  "main": "index.js",
  "scripts": {
    "test": "echo TODO",
    "build": "if [ -d dist ]; then rm -r dist; fi; ./node_modules/.bin/tsc src/*  --outDir dist"
  },
  "author": "Marcus Haddon",
  "license": "ISC",
  "dependencies": {
    "rxjs": "^6.5.1"
  },
  "devDependencies": {
    "@types/youtube": "0.0.37",
    "typescript": "^3.4.5"
  }
}
